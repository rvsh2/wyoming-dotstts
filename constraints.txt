# Vendored copy of dots.tts upstream constraints/recommended.txt so image
# builds do not depend on GitHub being reachable and cannot drift when
# upstream edits the file. Refresh manually when bumping the dots.tts pin.
torch==2.8.0
torchaudio==2.8.0
transformers==4.57.0
librosa==0.11.0
soundfile==0.13.1
numpy==2.2.6
pydantic==2.12.5
PyYAML==6.0.3
safetensors==0.8.0rc0
